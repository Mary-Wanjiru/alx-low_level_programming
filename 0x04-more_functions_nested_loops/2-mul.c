#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to miltiply
 *
 * Return: multiplication result
 */
int mul(int a, int b)
{
	return (a * b);
}
